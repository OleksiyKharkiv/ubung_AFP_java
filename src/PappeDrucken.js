let pappe = [3, 4, 0.5, 5];
let notaus = isNotaus();
function pappeDrucken(b, l, d){}

function isNotaus() {
    return undefined;
}

for (let i = 0; i < pappe[3]; i++) {
    if (notaus === false) {
        pappeDrucken(pappe[0], pappe[1], pappe[2]);
        notaus = isNotaus();
    }
}
